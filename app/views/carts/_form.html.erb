<%= form_for(@cart) do |f| %>
  <% if @cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
      <% @cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :asset_id_id, class: 'form-control', autofocus: true, placeholder: 'Barcode' %>
  </div>
  <div class="actions">
    <%= f.submit "Add" %>
  </div>
<% end %>
